# Cloudflare Pages 环境变量配置
# 在 Cloudflare Pages 仪表板的 Settings > Environment variables 中添加以下变量

# 只构建中文和英文语言包，大幅减少构建时间和产物大小
VITE_AVAILABLE_LOCALES=en,zh

# 禁用 PWA 自动更新（Cloudflare Pages 不需要 PWA）
VITE_PWA=false

# 跳过 TypeScript 类型检查（加快构建）
# 注意：本地开发时仍会进行类型检查
SKIP_TYPECHECK=true
